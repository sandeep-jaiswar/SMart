import styles from '../../../styles/Footer.module.css';

const Footer = () =>{
  return(
    <div className={`${styles[`footer-wrapper`]} w-100 d-flex `}>
      <div className=''>
      </div>
    </div>
  )
}

export default Footer;